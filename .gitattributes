# Normalize text files in the repo, and check out as LF everywhere
* text=auto eol=lf

# (Optional) keep Windows scripts as CRLF if you truly need it
*.bat text eol=crlf
*.cmd text eol=crlf

# Explicitly declare text files to ensure they are normalized
*.md text
*.yml text
*.yaml text
*.json text
*.txt text
*.sh text eol=lf
*.ps1 text

# Binary files
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
