# Debugging Session: {{symptom}}

session:
  created: "{{timestamp}}"
  status: "active"  # active | resolved | deferred | wontfix
  
# OBSERVATION

observation:
  symptom: "{{symptom}}"
  context: "{{context}}"
  expected: "{{expected}}"
  
  evidence:
    error_message: |
      # Exact error text
      
    logs: |
      # Relevant log entries
      
    steps_to_reproduce:
      - "Step 1"
      - "Step 2"
      - "Bug manifests"
      
  constraints:
    known_facts:
      - "What we know for sure"
      
    ruled_out:
      - "What we've already eliminated"

# HYPOTHESES

hypotheses:
  # - id: "hyp-001"
  #   claim: "The bug is caused by..."
  #   confidence: "medium"
  #   
  #   if_true:
  #     - "We would expect..."
  #     
  #   test:
  #     action: "Try..."
  #     expected: "Should see..."
  #     
  #   result:
  #     status: "pending"  # pending | confirmed | refuted | inconclusive
  #     observation: null
  #     learned: null

# TESTS

tests:
  # - id: "test-001"
  #   hypothesis: "hyp-001"
  #   action: "What we tried"
  #   result: "pending"
  #   learned: null

# PROGRESS

progress:
  current_hypothesis: null
  hypotheses_tested: 0
  hypotheses_refuted: 0
  
# ROOT CAUSE (when found)

root_cause:
  found: false
  explanation: null
  fix: null
  prevention: null  # How to prevent in future
